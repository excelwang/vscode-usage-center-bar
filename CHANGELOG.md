# Changelog

## 0.2.0

- Improved Marketplace metadata (`keywords`, `homepage`, `bugs`, `galleryBanner`, `files`, capabilities).
- Updated extension branding to **Codex Usage Timeline Bar**.
- Refined README for installation, configuration, endpoint contract, and publish workflow.
- Increased configurable `barLength` upper bound to `40`.
- Restored single-endpoint mode for proxy usage (`/api/codex/usage`) and removed ChatGPT-specific compatibility probing.

## 0.1.0

- 初始版本：状态栏进度条展示剩余用量
- 支持轮询与手动刷新
- 支持 base URL / endpoint / API key / JSON 路径配置
